{
  "name": "htmx-survey",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute htmx-survey --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute htmx-survey --remote --file=./schema.sql",
    "node": "node --experimental-sqlite server.cjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
